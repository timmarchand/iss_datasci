# Save your underlying code changes
git add .
git commit -m "update content tar pipelines"
git push origin main

# Publish the newly built HTML slides to the live website
quarto publish gh-pages


# Undo uncommited changes
git checkout .
